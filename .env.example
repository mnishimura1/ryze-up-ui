# Ryze Pro UI Environment Configuration

# API Base URL (defaults to https://api.ryze.pro if not set)
VITE_API_BASE=https://api.ryze.pro

# WebSocket URL (defaults to wss://api.ryze.pro/ws if not set)
VITE_WS_URL=wss://api.ryze.pro/ws

# Application environment
VITE_ENV=production
